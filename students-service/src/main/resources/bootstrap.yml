spring:
  application:
    name: students-service
  cloud:
    config:
      uri: http://localhost:8888 # Config Server URL
      fail-fast: true # Stop app if can't connect
      enabled: false
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
