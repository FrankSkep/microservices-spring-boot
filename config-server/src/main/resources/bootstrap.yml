server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/FrankSkep/school-management-microservices
          search-paths: config-data
  application:
    name: config-server